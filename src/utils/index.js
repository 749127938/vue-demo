// 表示把默认导入的替换成 getComponentRootDom 之后再具名导出
export { default as getComponentRootDom }
from "./getComponentRootDom";

// 表示把默认导入的替换成 showMessage 之后再具名导出
export { default as showMessage }
from "./showMessage";

export { default as formatDate }
from "./formatDate";

// 导出防抖函数
export { default as debounce }
from "./debounce";